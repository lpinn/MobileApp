import React from 'react'
import {Text} from 'react-native-elements'
import Button from '../components/Button'


const Cart = (props) =>  {
    // TODO adding products and grab its data
    // map out the state
};

export default Cart